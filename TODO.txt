Objectives
1. Make the statistics and plots into a fully functional react-powered web app
2. Make a data collection UI that simplifies tracking of statistics

Requirements
- React frontend
- Flask backend
- LLM api calls for constructive reccomendations
- Make a coding backend for all the python functions

To Fix:
- The plots look bad, name as the x-axis
- Percentages are by each name to shift to date and it should fix itself
- Analyst Commentary is adding the prompt stuff (erase)
- Team offense stats come up 4x idk why
- Make it so that when you click a plot it make a bigger pop-out
- Fix plots so the title isn't hidden by the legend